# Dependencias de desarrollo y CI: tests, coverage y linting.
# Instala tambien las de produccion. Uso:
#   py -3 -m pip install -r backend/requirements-dev.txt
-r requirements.txt

pytest>=9.0.3,<9.1
pytest-django>=4.10,<4.11
pytest-cov>=7.1,<7.2
coverage>=7.14,<7.15
ruff>=0.15,<0.16
